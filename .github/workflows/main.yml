name: Atualizador Híbrido (Preços 30min + Fundamentos Quinzenais)

on:
  schedule:
    # Roda a cada 30 minutos (ex: 09:00, 09:30, 10:00...)
    - cron: '*/30 * * * *'
  
  # Permite rodar manualmente para testes (botão "Run workflow")
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    # Timeout estendido para 6h para aguentar o scraping nos dias 01 e 16
    timeout-minutes: 360 
    
    steps:
      - name: 📥 Baixar repositório
        uses: actions/checkout@v3

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Instalar dependências
        run: |
          pip install yfinance pandas requests beautifulsoup4

      - name: 🤖 Rodar Robô Inteligente
        run: |
          python update_api.py

      - name: Salvar e Commit
        run: |
          git config --global user.name 'Robô Financeiro'
          git config --global user.email 'robo@financas.b3'
          git add dados_b3_atualizados.json
          git commit -m "Atualização Automática: $(date)" || echo "Sem mudanças para salvar."
          git pull --rebase origin main
          git push origin main
