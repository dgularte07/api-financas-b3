name: Atualizador Híbrido (Preços 30min + Fundamentos Quinzenais)

on:
  schedule:
    # Roda a cada 30 minutos
    - cron: '*/30 * * * *'
  
  # Permite rodar manualmente para testes
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    timeout-minutes: 360 
    
    steps:
      - name: 📥 Baixar repositório
        uses: actions/checkout@v3

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Instalar dependências
        run: |
          pip install yfinance pandas requests beautifulsoup4

      - name: 🤖 Rodar Robô Inteligente
        # AQUI ESTÁ A MÁGICA: Conecta o segredo da sua imagem ao script Python
        env:
          BRAPI_TOKEN: ${{ secrets.BRAPI_TOKEN }}
        run: |
          python update_api.py

      - name: 💾 Salvar e Commit
        run: |
          git config --global user.name 'Robô Financeiro'
          git config --global user.email 'robo@financas.b3'
          # Adiciona o arquivo gerado
          git add dados.json
          # Só faz commit se houver mudanças (evita erro se os preços não mudarem, ex: fim de semana/feriado)
          git commit -m "Atualização Automática: $(date)" || echo "Sem mudanças para salvar."
          git push
